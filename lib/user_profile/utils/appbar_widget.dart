import 'package:flutter/material.dart';

/// AppBar for Friend's profile
AppBar buildAppBar(BuildContext context) {
  return AppBar(
    title: const Text('Friend\'s Profile'),
    foregroundColor: Colors.black,
    backgroundColor: Colors.transparent,
    elevation: 0,
    actions: [],
  );
}
